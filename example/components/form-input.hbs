<div class="form-input">
  {{#if label}}<label for="{{name}}">{{label}}</label>{{/if}}
  <input type="{{type}}" name="{{name}}"{{#if value}} value="{{value}}"{{/if}}{{#if size}} width="{{size}}"{{/if}}{{#if placeholder}} placeholder="{{placeholder}}"{{/if}} />{{...}}
</div>
